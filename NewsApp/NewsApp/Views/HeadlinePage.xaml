<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="NewsApp.Views.HeadlinePage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:NewsApp.ViewModels"
    BackgroundColor="White">
    <ContentPage.Content>
        <StackLayout>
            <ActivityIndicator
                IsRunning="{Binding Loader}"
                IsVisible="{Binding Loader}"
                Color="Blue" />
            <ListView
                x:Name="Goods"
                ItemSelected="Goods_ItemSelected"
                ItemsSource="{Binding HeadLineLists}"
                SelectionMode="None">
                <ListView.BindingContext>
                    <local:HeadlineViewModel />
                </ListView.BindingContext>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Padding="10,0" Spacing="3">
                                <Label Text="{Binding title}" TextColor="Black" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>